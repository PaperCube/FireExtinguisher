#include "definitions.h"

const direction &FRONT = direction::FORWARD;
const direction &LEFT  = direction::LEFTWARD;
const direction &RIGHT = direction::RIGHTWARD;
const direction &BACK  = direction::BACKWARD;